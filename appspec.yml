version: 0.0
os: linux
files:
  - source: build/libs/
    destination: /apps/agileboard/
hooks:
  BeforeInstall:
    - location: scripts/setup_directory
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start_app
      timeout: 300
      runas: root
